name: Structural Freeze Snapshot

on:
  push:
    tags:
      - '*'

jobs:
  snapshot:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract tag name
        run: echo "TAG_NAME=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV

      - name: Generate structural index
        run: |
          echo "Generating snapshot for commit $GITHUB_SHA"
          git ls-tree -r --name-only "$GITHUB_SHA" | sort > "$TAG_NAME.index"
          sha256sum "$TAG_NAME.index" > "$TAG_NAME.index.sha256"

      - name: Commit snapshot
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add *.index*
          git commit -m "Auto structural snapshot for $TAG_NAME" || echo "Nothing to commit"
          git push origin HEAD:main
